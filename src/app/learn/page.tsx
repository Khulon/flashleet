// The Learn page content is rendered persistently in the layout
// via PersistentLearn so it never unmounts between navigations.
// This route just needs to exist so the Nav can link to /learn.
export default function LearnRoute() {
  return null;
}
